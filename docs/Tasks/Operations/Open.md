# Open

**Type:** `Open`

Opens a file/program, optionally with arguments and elevation.

## Tips

- When using `runAsAdmin`, scope the trigger carefully (for example a Result selector that only matches `*.exe`, `*.bat`, `*.cmd`).
